#include <tunables/global>

profile huntarr flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities (minimal required for s6-overlay and Python)
  capability setgid,
  capability setuid,
  
  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Access to options.json and other files within your addon
  /data/** rw,

  # Access to mapped volumes
  /config/** rw,
  /share/** rw,
  /ssl/** r,

  # Config access
  /etc/hosts r,
  /etc/resolv.conf r,
  /etc/ssl/openssl.cnf r,

  # DNS and HTTP/HTTPS network access
  network inet udp,
  network inet tcp,
  network inet6 udp,
  network inet6 tcp,

  # Allow running applications
  /usr/local/bin/** ixr,
  /app/** ixr,
  
  # Python
  /usr/bin/python* ixr,
  /usr/lib/python*/** r,
}
